#login
  - if @current_user
    %p.welcome Welcome, #{@current_user.name}. <a href='logout_path'> Logout</a>
  - else
    %p.login= link_to 'Log in with your Twitter account', '/auth/twitter?force_login=true'
